{# templates/wild/category.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}{{ category }}{% endblock %}

{% block body %}

    <div class="container-fluid row">
    {% for program in programs %}
        <div class="col-lg-3 offset-1">
            <h5 class="mt-0">{{ program.title }}</h5>
            <img src="{{ program.poster }}" class="picture_series" width="300">
            <p>{{ program.summary }}</p>
        </div>

    {% endfor %}

    <button type="button" class="btn btn-dark mr-2"><a class="btn-return" href="{{ path('app_index') }}">
        Retour à l'accueil
        </a></button>


{% endblock %}